@echo off
rem Windows counterpart of env.sh.eex: the shipped agent binary (`lapsus`) defaults
rem to the live community coordinator unless LAPSUS_COORDINATOR_URL is already set.
if "%RELEASE_NAME%"=="lapsus" (
  if "%LAPSUS_COORDINATOR_URL%"=="" set LAPSUS_COORDINATOR_URL=wss://lapsus.pyrates.io
)
